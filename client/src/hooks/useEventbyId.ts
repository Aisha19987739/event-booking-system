// import { useQuery } from 'react-query';
// import { getEventById } from '../sevices/eventService'; // تأكد أن لديك هذا المسار
// import   type {Event} from '../types/Event'; // تأكد أن لديك هذا المسار




// export const useEventbyId = (id: string | undefined) => {
//   return useQuery<Event | null>({
//     queryKey: ['event', id],
//     queryFn: () => getEventById(id!),
//     enabled: !!id,
//     retry: 1,
//   });
// };
